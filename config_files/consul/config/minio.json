{
  "service": {
    "name": "minio",
    "id": "minio-9000",
    "address": "minio",
    "port": 9000,
    "tags": ["profile:ai", "objectstore:minio", "s3api", "http:9000"],
    "checks": [
      {
        "name": "minio-ready",
        "http": "http://minio:9000/minio/health/ready",
        "method": "GET",
        "interval": "10s",
        "timeout": "5s"
      }
    ]
  }
}
