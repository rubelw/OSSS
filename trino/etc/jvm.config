# FIRST: unlock if you keep any experimental flags
-XX:+UnlockExperimentalVMOptions

# Memory (or use JAVA_TOOL_OPTIONS)
-Xms512m
-Xmx2g

# Safe G1 options (non-experimental) and misc
-XX:+UseG1GC
-XX:MaxGCPauseMillis=200
-XX:+UseStringDeduplication
-XX:+ExitOnOutOfMemoryError

# If you keep experimental ones, they must come AFTER the unlock
# -XX:G1MaxNewSizePercent=60
# -XX:G1NewSizePercent=5
